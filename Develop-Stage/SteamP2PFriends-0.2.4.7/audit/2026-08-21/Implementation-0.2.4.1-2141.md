# SteamP2PFriends 0.2.4.1 M1 运行验收通过记录

## 需求执行概述

关闭 Multi-Observer M1 `ItemGenerationAuthorityAdapter` 的 listen-host 双机运行验收。M1 使远端观察者（包括待审核玩家）在原生 `ItemManager.onRegionUpdated(step 5)` 中按区域需求触发生成，保留区域单写者 gate，并在审批状态变化时保持同一世界实例。

## 验收输入

- 客机 UMM 包：`UMM-诊断包_20260821_204907`。
- 房主 UMM 包：`UMM-诊断包_20260821_204939`。
- 当前实验区 Debug 候选 SHA-256：`1F02A37DF9F944A9AD33B3CD32283ABF2AFB697934E5C0A0DE532E2813CF509A`。
- 开发者受控部署担保：双端从同一候选文件夹手动复制，已人工核对哈希相同。UMM 原始包未自行记录该数值，因此该身份事实来源于开发者部署声明，不伪装为日志字段。

## 运行验收矩阵

| 验收项 | 结论 | 证据 |
| --- | --- | --- |
| M1 远端 step 5 链路登记 | PASS | 房主日志 `:104-105`；远端 `M1-Item allow` 紧随 `askItems` 发送于 `:306-315` |
| 待审核客机远区物品可见 | PASS | 开发者实测：待审核客机以软隔离状态在远区看到生成的一把锤子 |
| 待审核客机不能拾取 | PASS | 开发者实测：审核前无法拾取该锤子 |
| 审批不重生、不替换实例 | PASS | 开发者实测：审批后回到审核前位置，仍为同一把锤子；房主日志也记录授权只改变 `AuthorizationChanged`，不移除 observer（`:438-443`） |
| 单写者防二次生成 | PASS | 已提交 region 在后续需求中为 `Committed` 后 skip（房主日志 `:328-342`） |
| 客机拾取后的权威移除 | PASS | 开发者实测：客机审批后拾取锤子，房主返回桌面时该锤子不可见 |
| 房主回区不覆盖旧状态 | PASS | 日志 `:456-465` 记录同区域仍 `Committed` 后 skip；人工实测确认锤子未重新出现 |
| 同一二进制部署 | PASS（开发者部署担保） | 双端由开发者从同一文件夹手动复制并核对相同哈希；原始 UMM 包只保留版本 `0.2.4.1` |

## 先前证据不足报告的处置

`RuntimeFix-0.2.4.1-2110.md` 保留为当时仅基于 UMM 日志做出的 FAIL 快照，不覆盖、不删除。其阻断项 1、2、3、4 已分别由本轮开发者受控双机观察和同文件夹同哈希部署担保关闭。

## 编译与独立审核

- 本次是运行验收收口与归档，没有修改源码或 DLL，未重新编译。
- 当前候选的既有静态记录：Debug/Release `0 errors / 0 warnings`，自动化 `81/81 PASS`，见 `Implementation-0.2.4.1-2030.md`。
- 独立运行日志复核曾判定“日志证据不足但未发现运行故障”；本记录基于开发者明确补充的受控实测结论关闭余项。

## 最终结论

**PASS。** M1 `ItemGenerationAuthorityAdapter` 已完成静态、自动化与开发者验收的双机运行闭环，冻结为 `SteamP2PFriends-0.2.4.1`。实验区已具备进入 M2 的前置条件；本报告不自行启动 M2 构建。
